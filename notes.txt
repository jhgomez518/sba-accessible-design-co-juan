
Flexbox:

flex-direction: row; (default, don't need to explicitly write if this is intended)
flex-direction: column;

//justify-content affects items horizontally in flex-direction: row, that is, this is default behavior
//justify-content affects items vertically in flex-direction: column

justify-content: flex-start;
justify-content: flex-end;
justify-content: center;
justify-content: space-around;
justify-content: space-evenly;

//align-items affects items vertically in flex-direction: row, that is, this is default behavior
//align-items affects items horizontally in flex-direction: column

align-items: center;
align-items: flex-start;
align-items: flex-end;

//for a quick refresh, do first seven levels of: https://flexboxfroggy.com/

